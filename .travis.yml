language: php
php:
  - '7.0'
services:
  - mysql
install:
  - sudo apt update -qq
  - sudo apt install -y apache2 libapache2-mod-fastcgi

script:
  - php -f ci/server/setup.php testing-framework
  - php -f ci/run.php master